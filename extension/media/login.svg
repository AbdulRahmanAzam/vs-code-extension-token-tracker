<svg xmlns="http://www.w3.org/2000/svg" width="500" height="420" viewBox="0 0 500 420">
  <defs>
    <linearGradient id="lBg" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#0a0a0a"/>
      <stop offset="100%" stop-color="#0d1117"/>
    </linearGradient>
    <linearGradient id="lCard" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stop-color="#111a14"/>
      <stop offset="100%" stop-color="#0d120f"/>
    </linearGradient>
    <filter id="lGlow"><feGaussianBlur stdDeviation="3" result="b"/><feMerge><feMergeNode in="b"/><feMergeNode in="SourceGraphic"/></feMerge></filter>
    <filter id="lShadow"><feDropShadow dx="0" dy="4" stdDeviation="12" flood-color="#00ff8818"/></filter>
  </defs>

  <!-- Background -->
  <rect width="500" height="420" fill="url(#lBg)" rx="12"/>
  <!-- Grid -->
  <g opacity="0.04">
    <pattern id="ld" width="24" height="24" patternUnits="userSpaceOnUse">
      <circle cx="2" cy="2" r="0.7" fill="#00ff88"/>
    </pattern>
    <rect width="500" height="420" fill="url(#ld)"/>
  </g>

  <!-- Login Card -->
  <rect x="100" y="40" width="300" height="340" rx="14" fill="url(#lCard)" stroke="#00ff8833" stroke-width="0.5" filter="url(#lShadow)"/>

  <!-- Theme toggle -->
  <rect x="340" y="55" width="38" height="18" rx="9" fill="#00ff8815" stroke="#00ff8833" stroke-width="0.5"/>
  <circle cx="367" cy="64" r="6" fill="#00ff88" opacity="0.8"/>
  <text x="365" y="67" font-family="sans-serif" font-size="7" fill="#000" text-anchor="middle">🌙</text>

  <!-- Logo -->
  <text x="250" y="100" font-family="monospace" font-size="36" fill="#00ff88" text-anchor="middle" filter="url(#lGlow)">⚡</text>
  <text x="250" y="130" font-family="monospace" font-size="18" font-weight="bold" fill="#e0e0e0" text-anchor="middle">Token Tracker</text>
  <text x="250" y="150" font-family="monospace" font-size="10" fill="#555" text-anchor="middle">// admin_dashboard — authenticate</text>

  <!-- Username field -->
  <text x="135" y="185" font-family="monospace" font-size="10" fill="#00ff88" opacity="0.7">→ Username</text>
  <rect x="130" y="192" width="240" height="34" rx="6" fill="#0a0a0a" stroke="#00ff8833" stroke-width="0.5"/>
  <text x="145" y="213" font-family="monospace" font-size="11" fill="#555">admin</text>

  <!-- Password field -->
  <text x="135" y="248" font-family="monospace" font-size="10" fill="#00ff88" opacity="0.7">→ Password</text>
  <rect x="130" y="255" width="240" height="34" rx="6" fill="#0a0a0a" stroke="#00ff8833" stroke-width="0.5"/>
  <text x="145" y="276" font-family="monospace" font-size="11" fill="#555">••••••••</text>

  <!-- Login button -->
  <rect x="130" y="305" width="240" height="36" rx="7" fill="#00ff88" opacity="0.9"/>
  <text x="250" y="328" font-family="monospace" font-size="12" fill="#000" text-anchor="middle" font-weight="bold">⚡ Authenticate</text>

  <!-- Footer text -->
  <text x="250" y="365" font-family="monospace" font-size="8" fill="#444" text-anchor="middle">Centralized Token Management System v1.0</text>
</svg>